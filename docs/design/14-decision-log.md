# 14. Decision Log

| Date       | Decision                                                            |
|------------|---------------------------------------------------------------------|
| 2026-05-19 | Backend: Rust + Axum. Compile-time plugins via Cargo workspace.     |
| 2026-05-19 | API contract: Connect-RPC (Buf), per-plugin `.proto` files.         |
| 2026-05-19 | Frontend: React + TypeScript + TanStack Router + TanStack Query + Vite. SPA mode. |
| 2026-05-19 | Plugins are dual-packaged (Cargo crate + pnpm package + proto).     |
| 2026-05-19 | Cross-plugin component sharing via ES imports (required) + typed registry (optional). |
| 2026-05-19 | `junius` composes FE into one Vite project; build artifact is embedded in the Rust binary via `rust-embed`. |
| 2026-05-19 | Deployment artifact: one self-contained Rust binary.                |
| 2026-05-19 | Plugin manifest is `plugin.toml`, TOML format. Single source of truth for inter-plugin metadata. v1 schema in [06-plugin-shape.md](06-plugin-shape.md) §6.1. |
| 2026-05-19 | Permissions declared in manifest, enforced in code. Audit-friendly without code analysis. |
| 2026-05-19 | Plugin capabilities (`db.read`, `email.send`, etc.) are audit-only in v1. Runtime enforcement deferred. |
| 2026-05-19 | Management CLI named `junius`. Owns composition, codegen, scaffolding, validation, build, dev. Replaces the "kickstart" concept; kickstart is now just a subset (`compose` + `build`). |
| 2026-05-19 | Sync strategy: `junius` fully overwrites generated files; uses marker comments to manage co-edited files (`Cargo.toml`, `package.json`, `buf.yaml`); never touches human-only files. |
| 2026-05-19 | `junius dev` is the one-command dev flow (Vite + cargo + watchers + buf-on-change + sync-on-manifest-change). |
| 2026-05-19 | Plugin directory layout is fixed by convention; `[paths]` block removed from the manifest. Plugins omit directories they don't need. |
| 2026-05-19 | No per-plugin versioning and no dependency version constraints. Single-version monorepo policy; `manifest_schema` is the only versioning concept retained. |
| 2026-05-19 | Cross-repo plugins are integrated via git submodules under `plugins/<name>/`; `junius` treats them identically to in-tree plugins. |
| 2026-05-19 | Auth, user management, and RBAC live in the host (`platform/`), not as plugins. Plugins consume identity primitives via `junius-sdk`. |
| 2026-05-19 | Repository / deployment split: the source monorepo is the codebase (no `platform.toml`); deployments live in separate directories (their own repos or server dirs) that pin the source revision and declare enabled plugins. `junius build` in a deployment dir produces that deployment's binary. |
| 2026-05-19 | Source resolution: deployment's `[source]` block points at a git URL+rev or a local path; `platform.lock` records the resolved revision. |
| 2026-05-19 | Dev sandbox: `dev/platform.toml` inside the source monorepo for local development. Not a real deployment — a developer convenience used by `junius dev`. |
| 2026-05-19 | Provided platform infra (v1): Database, Object Storage, Background Jobs, Email, Config/Secrets, Telemetry. Exposed as typed `PluginContext` handles gated by `[requires.capabilities]`. Cache/search/realtime deferred to v2. |
| 2026-05-19 | Database: PostgreSQL + sqlx with committed `.sqlx/` prepared-query cache. One Postgres schema per plugin; host owns `platform.*` and `meta.*`. |
| 2026-05-19 | Always-apply migrations: every deployment runs the full migration set from its pinned source revision regardless of which plugins are enabled. Enable/disable is purely a code concern; schemas always exist. |
| 2026-05-19 | Cross-plugin FKs and JOINs allowed across both required and optional deps. FKs into optional-dep schemas must be nullable. Cross-plugin writes via SQL allowed; invariants enforced via Postgres triggers when needed. `CASCADE` on cross-plugin FKs forbidden. |
| 2026-05-19 | `[exposes.tables]` declares public R+W tables; private tables (everything else) evolve freely. Consumer plugins declare `[dependencies.<dep>].tables = [...]`. |
| 2026-05-19 | Postgres role enforcement from day one: each plugin runs as `role_<plugin>` with grants computed from manifests by `junius migrate`. One pool per role. Migration runner uses privileged `platform_migrator` role. |
| 2026-05-19 | Migration ordering: host first; plugin migrations as a topo sort over (within-plugin filename order + explicit `-- @requires <plugin>:<migration>` edges). Manifest plugin deps do NOT infer migration ordering. |
| 2026-05-19 | `meta.migrations` records applied migrations with `BIGSERIAL id`, `plugin`, `migration_name`, `checksum` (SHA-256), `applied_at`. |
| 2026-05-19 | Down migrations optional and discouraged; forward-fix is the recommended discipline. |
| 2026-05-19 | Schema evolution: `[exposes.tables]` is the compatibility surface; breaking changes coordinated in the same source revision across all consumers. v1 enforcement = Layer 1 (`junius check` static) + Layer 2 (CI integration test with ephemeral Postgres). Layer 3 (snapshot diff with `@breaking-change` annotation) deferred. |
| 2026-05-19 | Authentication: OIDC against Authentik (configurable per deployment). Platform creates server-side sessions in `platform.session` (Postgres); session cookies are HTTP-only, Secure, SameSite=Lax. OIDC tokens stored encrypted in the session for downstream proxying if needed. Tokens-in-localStorage explicitly unsupported. |
| 2026-05-19 | Groups and roles are first-class. `platform.group` = real-world unit (committee, caucus, etc.). `platform.group_role` is defined *within* a group; "Chair of Committee X" and "Chair of Caucus Y" are distinct rows. A user has **at most one role per group**. Roles own the capability-permission set in `platform.role_permission`, referencing strings declared in manifests' `[permissions]`. |
| 2026-05-19 | Groups have no relations among themselves — no hierarchy, no parent/child, no inheritance. Flat membership only. |
| 2026-05-19 | Two-layer authorization: (1) **capability gate** via manifest-declared permission strings, enforced at handler / repo method via `Has<X>` bounds — user must hold the capability in *some* group; (2) **resource-scoped filter** via `platform.user_can_access(...)` joined into every repo query — resolves ownership, group-role grants, explicit shares, and public visibility. Inaccessible rows don't appear in results. |
| 2026-05-19 | Resource ownership recorded in `platform.resource_principal`; exactly one of `owner_user_id` or `owner_group_id`. **Ownership kind is per-resource-instance**, chosen by the plugin at creation time — some resource types are conceptually group-owned (committee meetings), others user-owned (personal documents, vote ballots cast). Owners have implicit full access regardless of role permissions. |
| 2026-05-19 | Sharing v1: only the current owner can share a resource. `platform.resource_share` rows grant a specific permission to a user, a group, or `principal_kind = 'public'` (platform-wide visibility, opt-in per resource). Finer-grained "who can share what" rules deferred. |
| 2026-05-19 | `resource_kind` strings in `platform.resource_principal` and `platform.resource_share` use the `<plugin>:<table>` format (e.g. `speakers:speaker`, `events:meeting`). |
| 2026-05-19 | Frontend `User` carries `memberships[]` with each group's role and capability-permission set. `useHasPermission(p)` returns true if any membership grants `p`. Per-resource access decisions come from server-supplied flags on resource payloads (e.g. `viewerCanEdit`); the client does not recompute access locally. |
| 2026-05-19 | Backend plugin interface: `Plugin` trait — object-safe, async, `metadata()` + `routes()` required, `on_startup`/`on_shutdown`/`jobs()` defaulted no-op. v1 schema in [11-backend-plugin-interface.md](11-backend-plugin-interface.md). |
| 2026-05-19 | Plugin convention: every plugin exposes `pub fn new(...) -> Self`; constructor may take host-injected args. `junius`-generated glue calls it. |
| 2026-05-19 | Plugin metadata produced by `junius_sdk::plugin_metadata!()` proc-macro reading `plugin.toml` at compile time. No source-tree codegen for metadata. |
| 2026-05-19 | `junius check` verifies every plugin crate invokes `plugin_metadata!()` exactly once. |
| 2026-05-19 | Permission system: macro-generated zero-sized marker types per declared permission. Undeclared permissions are compile errors. `permissions!()` macro combines markers via `And<A, B>`. OR combinators deferred. |
| 2026-05-19 | `PluginContext<S, P>` is generic in `junius-sdk` over plugin-defined state `S` and permission witness `P`; `Clone` (resources `Arc`-wrapped). Plugin authors define a per-plugin type alias `<PluginName>Ctx<P>` and the `#[derive(PluginCtx)]` extractor verifies `P` while assembling state. |
| 2026-05-19 | Repository pattern enforced by `junius-sdk`: no public `db()` accessor on `PluginContext`; raw `PgPool` not in public API. Data access only via `#[derive(Repository)]` types within `#[impl_repository(...)]` blocks. Methods gated by `Has<X>` bounds. Same pattern (`Bucket`) for object storage. |
| 2026-05-19 | Capability gating runtime-only in v1: undeclared capability handles return `Err(PluginError::CapabilityNotDeclared(...))`. Compile-time gating deferred. |
| 2026-05-19 | Connect-RPC routing: auto-generated via `connect_rs::router_for(service)`. |
| 2026-05-19 | RPC method permissions declared in `.proto` via `option (platform.requires) = "..."`. Enforced server-side by host interceptor; codegen produces `<PluginName>Ctx<P>` handler signatures (the per-plugin type alias from [11-backend-plugin-interface.md](11-backend-plugin-interface.md) §11.6); surfaced in TS client; `junius check` validates strings against manifest `[permissions]`. |
| 2026-05-19 | `Plugin::routes` returns an unprefixed `Router`; host nests under `/rpc/<plugin>` / `/h/<plugin>` based on metadata prefixes. |
| 2026-05-19 | Background jobs registered via `Plugin::jobs() -> Vec<JobHandler>`; job names namespaced `<plugin>.<job_name>`; jobs invoke permission-gated repos via a `system_context()` helper (mechanism TBD). |
| 2026-05-19 | `PluginResources` (raw, pre-scoped DB pool, storage, jobs, email, etc.) is what `Plugin::routes` and lifecycle hooks receive. `PluginContext<S, P>` becomes a generic struct in `junius-sdk` parameterized by plugin-defined per-request state `S` (typically a bundle of typed repositories) and permission witness `P`. Each plugin defines its own `<PluginName>Ctx<P> = PluginContext<<PluginName>State<P>, P>` type alias. |
| 2026-05-19 | `#[derive(PluginCtx)]` macro generates each plugin's per-request extractor, performing permission verification, repository instantiation (with the request's user), and context assembly. Replaces `SpeakerRepo::from_ctx(&ctx)` in handler bodies; handlers access repos directly as `ctx.state.<repo>`. |
| 2026-05-19 | Frontend plugin contract: plugin is a pnpm workspace package (`@junius/plugin-<name>`) exporting a fixed surface from `src/index.ts` (`buildRoutes`, public components, domain types). No `Plugin` interface to implement; composition is via named exports + `junius`-generated shell aggregators. |
| 2026-05-19 | All TypeScript codegen lives in a single workspace package `@junius/generated` with subpath exports per plugin (`@junius/generated/<plugin>`, `@junius/generated/<plugin>/rpc`, `@junius/generated/shared/proto`). Plugin source trees contain zero generated files — mirroring the backend's macro-based approach. No deployment-bound aggregate packages (e.g. no `@platform/permissions`) since enablement isn't bound to the source repo. |
| 2026-05-19 | Routes composition: each plugin exports `buildRoutes(parent: AnyRoute)` returning a TanStack Router subtree; `junius` generates the shell's composed route tree from each plugin's `buildRoutes` plus the plugin's `route_prefix` from `plugin.toml`. |
| 2026-05-19 | Permissions on the frontend: per-plugin `Permission` union from `@junius/generated/<plugin>`; `@junius/sdk` hooks (`useHasPermission`, `requirePermissions`, etc.) are generic over the union; cross-plugin permission imports require a declared manifest dep, enforced by `junius check`. |
| 2026-05-19 | RPC on the frontend: shell owns the single Connect transport; each plugin gets a narrow `rpc` namespace generated in `@junius/generated/<plugin>/rpc` containing only methods declared in its manifest (own services + `[dependencies.<dep>].rpc_methods`). Mirrors the backend's repository pattern: single source of truth, per-consumer narrowed compile-time surface. v1 enforcement via `junius check`; tsconfig `paths` allowlist deferred. |
| 2026-05-19 | Design system: Tailwind + Radix UI primitives wrapped in `@junius/design` (Button, Card, Stack, Modal, Form, etc.). One workspace-root `tailwind.config.ts`; tokens via Tailwind theme + CSS variables. Plugins never write raw `<button>` or HTML form elements. |
| 2026-05-19 | Host shell composition: thin `main.tsx` (~50 LoC) wrapping `AuthProvider`, `TransportProvider`, `QueryClientProvider`, `ComponentRegistryProvider`, `RouterProvider`. Routes + component registry imported from `junius`-generated aggregators in `src/generated/`. |
| 2026-05-21 | Project renamed from "Political Platform" to **Junius**. CLI renamed `platctl` → `junius`; host server binary named `juniusd`. Rust crates `platform-sdk` → `junius-sdk` and `platform-sdk-macros` → `junius-sdk-macros`. npm scope `@platform/*` → `@junius/*`. **Intentionally kept**: the DB schema names (`platform.*`), the proto package `platform.v1` (annotations), and the host directory `platform/` — "platform" describes the underlying substrate role; "Junius" is the product on top. |
| 2026-05-25 | Connect-RPC server re-platformed from the M05 hand-rolled unary module onto `connectrpc` (Anthropic's Tower-based Connect server) with `buffa` zero-copy protobuf message types, replacing `prost`. Generated service traits + axum integration (`register(...).into_axum_router()`); per-request caller + plugin resources flow in via request extensions (`RequestContext::extensions`). Forced a workspace bump to Rust 1.88 (connectrpc MSRV) and axum 0.7 → 0.8. Wire protocol unchanged (Connect), so the TS client/FE is untouched. Supersedes the 2026-05-19 "hand-rolled, swap if a robust crate emerges" stance from M05. |
| 2026-05-25 | M07 landed the data/permission layer: `#[repository]` (attribute, not derive — it injects fields) gates SQL behind `Has<X>` permission witnesses (HList index-membership; `#[impl_repository]` injects the index per method); `plugin_metadata!()` emits permission marker types; `#[derive(PluginCtx)]` + a blanket `FromRequestParts` over `PluginContext` build the per-request, permission-checked context. Repos use compile-time `sqlx::query!` with a committed `.sqlx/` (CI `SQLX_OFFLINE`); runtime queries allowed for dynamic SQL. RPC `option (platform.v1.requires)` is enforced by a host axum guard over `/rpc` reading a `junius sync`-generated table (`PROTO.REQUIRES.UNDECLARED` validates the strings); handlers build `Ctx<P>` via `from_rpc`. No connectrpc interceptor (buffa has no runtime method-option reflection). |
| 2026-05-25 | M08 landed per-instance access control. `platform.user_can_access` recreated SECURITY DEFINER + new SECURITY DEFINER `platform.record_owner` so least-privilege plugin roles can read access + record ownership (atomically, in their own tx) without direct write on the host authz tables; `resource_share` gained an `id` PK. SDK `Authz` (on `PluginResources`, request-scoped) does owner-checked + audited `share`/`unshare` on the platform pool. Repo `list`/`get` filter by `user_can_access` and return server-computed `viewer_can_edit`/`viewer_can_share`; a note the caller can't access is `NotFound` (no existence leak). hello gained a `note` table + `hello.v1.NoteService` demo. FE consumes the flags directly (never recomputes access). |
| 2026-05-25 | M09 landed cross-plugin composition (`greetings` → required `hello` + optional `widgets`). Tooling unified behind `go-task` + a root `Taskfile.yml` (CI = `task ci`). Multi-plugin RPC: each plugin's `register_rpc` folds services into one shared `connectrpc::Router`; the host injects the right per-plugin `PluginResourceCtx` per request by service FQN (generated `RPC_SERVICES`). Narrow FE RPC namespace: `@junius/generated/<plugin>/rpc` exports a `rpc` object with only the plugin's own methods + each `[dependencies.<dep>].rpc_methods` (built from protoc-gen-es `GenService.method.<name>`). Component registry typing uses a **per-consumer generated `useComponent` wrapper** (local `ComponentRegistry` interface + `ComponentType<FooProps>` from the producer's cycle-free `./lib/*` subpath), not `declare module '@junius/sdk'` augmentation (load-order/cycle sensitive); the SDK `useComponent` is the untyped lookup. `junius check` gained cross-plugin rules (`DEP.UNDECLARED`, `RPC.UNDECLARED`, and `sqlparser`-based `SQL.REQUIRES.MISSING`/`FK.OPTIONAL.NULLABLE`/`FK.CROSS.CASCADE`). Cross-plugin grants + `-- @requires` migration ordering (already built in M06) are exercised, not added. E2E: deterministic vitest for the widgets enabled/disabled fallback; Playwright wired but gated behind `JUNIUS_E2E` (needs the dev stack + login). |
| 2026-05-25 | M10 landed the four infra capabilities behind vendor-neutral SDK traits (no vendor crate in `junius-sdk`; the host bridges). **Jobs → RabbitMQ** (`lapin` 3 + `deadpool-lapin`, `JobBackend` trait), not apalis: `meta.job_run` is durable observability, RabbitMQ owns the queue; in-proc worker with durable queue + DLQ per job, retry-by-republish to a bounded attempt count then dead-letter, graceful drain. **Telemetry → Grafana LGTM** export-only (`tracing` stays the API; OTel 0.32 set / tracing-opentelemetry 0.33): spans→traces, events→logs (appender), metrics via an SDK `MetricSink`. **Email → lettre** (`Transport`: log/mailpit/SMTP/Resend), `from`-domain validation. **Storage → rust-s3** behind an `ObjectStore` trait (NOT aws-sdk-s3: its release needs rustc ≥1.91 vs the toolchain's 1.88; rust-s3 is the sanctioned fallback, swap localized to `platform/src/storage/`): manifest logical buckets → deployment physical buckets → `platform.object` rows (SECURITY DEFINER `record_object`, mirroring M08); presigned + juniusd-mediated URL negotiation with HMAC-signed op/object-scoped tokens served at `/api/storage`. Typed bucket surface generated by `plugin_metadata!()` from the manifest (`buckets::Bucket` enum impl `BucketName`), not a `#[bucket]` macro. Capability gating is runtime (`email.send`/`job.enqueue`/`storage.read`/`storage.write`); `system_context` builds caller-less per-plugin resources for job handlers. Host audit-prune interval task. `junius check` gained `STORAGE.BUCKET.UNMAPPED`. Deferred: hello `AttachToNote` RPC (proto + FK migration + sqlx/FE regen). |
| 2026-05-25 | M11 landed the deployment workflow: a deployment is a directory with only `platform.toml` (+ secrets + `platform.lock`); `junius build` resolves `[source]` (local `path` or a git `repo`+`rev`), composes glue into that source, builds `-p platform --release --features embed-frontend`, and copies the `juniusd` binary to `<deployment>/platform`. **git resolution shells out to the `git` CLI** (not `gix`) into `~/.cache/junius/sources/<sha256(url+rev)>`; `junius cache prune` evicts by mtime. **`platform.lock`** records the resolved rev + **blake3** content hashes (whole tree + per-plugin), strict by default with `--update-lock` to refresh; it hashes `path` sources too (so the in-repo example's lock is gitignored + CI uses `--update-lock`). `sync` was refactored to a `run_in(source_root, …)` engine (dev `sync` still uses `.`); the `juniusd`→`platform` artifact name differs by design (copy). A **`develop`** default cargo feature gates the source-tree-only subcommands (`new`/`sync`/`dev`) so `cargo build -p junius --no-default-features` ships an end-user CLI (`check`/`build`/`migrate`/`plugin`/`cache`); it trims the command surface only — `tokio`/`regex` are shared, so no dependency drops. Secret resolver gained `file:PATH` (mounted secrets); `vault:` stays an explicit unsupported error. `junius plugin enable/disable` edit `[plugins].enabled` with `toml_edit` (comments preserved) + dependency checks. `juniusd --check-config` (and `junius build --check-config`) validate config without a DB. CI gained an `example-deployment` job (cheap `--check-config`; full build gated behind `JUNIUS_E2E`). |
| 2026-05-25 | M12 hardened the static-analysis + CI gate. `junius check` gained three rules: **`SQL.PRIVATE_TABLE_ACCESS`** (a cross-plugin `schema.table` ref — in a migration *or* an `sqlx::query*!` macro — must be exposed by the owner and declared in `[dependencies.<owner>].tables`); **`SQL.EXPOSED.NO_BREAKING`** (a breaking change — DROP/RENAME COLUMN, DROP TABLE, ADD NOT NULL, narrowing type — to an exposed table in a migration *added vs `--base`* is rejected unless every declaring consumer ships a migration in the same diff); **`FE.EXPORTS.MATCH_MANIFEST`** (every `[exposes.components.X]` must be a named export of `frontend/src/index.ts`). Implementation deviates from the sketch: **sqlparser is 0.52** with its `visitor` feature on (`visit_relations` collects table refs generically; FK `REFERENCES` targets are pulled out explicitly since the visitor skips them in `CREATE TABLE`); **repo-query SQL is extracted with `syn`** (parse `query!`/`query_as!`/`query_scalar!` macro tokens, first string literal = SQL), not regex; type-narrowing is **conservative** (any type change breaks unless widening to `TEXT`/unbounded `VARCHAR`/`BIGINT`); the breaking rule diffs `git diff --diff-filter=A <base>...HEAD` ∪ untracked and **no-ops outside a git repo**. Diagnostics gained a rule-ID-derived doc link (`#<rule>` in `13-M12-hardening.md`); precise `line:col` deferred. CI was split into **four parallel jobs** (`lint`/`check`/`integration`/`deployment`); new gates **`buf breaking`** (in `check`, against `main`) and **`cargo sqlx prepare --check`** (in `integration`, after `junius migrate up` against a Postgres service — it needs a live migrated DB, a deviation from the spec's placement in `check`). `MIGRATIONS.CHECKSUM` needs no new code (runtime `migrate up` enforces it, now in `integration`). **Layer 3** (schema-snapshot diff) stays deferred. |
| 2026-05-25 | M13 shipped `events`, the first real domain plugin — events owned by a **user or a group**, **private/public**, with sign-up invite pages (slot limits, manual open/close, per-field visibility toggles, group pre-sign-up + opt-out), a confirmation-email **background job**, and revocable **iCalendar** export (single-event ACL download + key-authed personal/group subscription feeds + a per-group public toggle). Visibility is a `visibility` column the reads bypass the ACL for (`visibility='public' OR user_can_access(...)`), not a synthetic "everyone" principal. Two **unauthenticated** surfaces were introduced (the host session middleware is already pass-through, so no host change): the SPA's login-optional **public routes** (manifest `public_routes=true` → `buildPublicRoutes` mounted at `/i/<plugin>` outside the authed shell; `AuthProvider` `publicPaths` + one `goToLoginUnlessPublic` helper both redirect paths consult), and ungated handlers that build the **no-permission witness** (`Ctx::<()>`) or a caller-less repo and let the SQL ACL decide (public read/write methods live in a plain `impl<P>` block, callable on `Repo<()>`). Feed keys are unguessable, **stored hashed** (SHA-256), subject-scoped, and revocable; feeds resolve against the subject's live entitlements. Also landed as reusable platform pieces: an **SDK `Groups`** directory accessor with an **authorization-checked `members(group, as_caller)`** (non-members get `PermissionDenied`); a compile-time-safe `PluginResourceCtx` (a `pub`-field struct built by **named struct literal**, no constructor, so adding a host handle is a compile error everywhere); and a **`@junius/design` forms library** (`Form`/`createFormField` over react-hook-form + zod, with `Input`/`Select`/`Textarea`/`DateTime`/`DateRange`). `events.event` is the only exposed table; invite/signup/calendar_token/group_calendar stay private. The build doubled as a **plugin-authoring dogfooding pass** — friction captured in [`docs/impl/14-M13-events-plugin.friction.md`](../impl/14-M13-events-plugin.friction.md) and a worked-example [`docs/plugin-authoring-guide.md`](../plugin-authoring-guide.md). |
| 2026-05-26 | M16 closed the four M13 dogfooding follow-ups (A/B/C/D in the joint friction triage). **C** `User::has_permission_in_group(group, permission)` lifts the per-group authz check `events` was hand-rolling over `user.memberships`. **B** `Authz::forget_resource(&mut tx, kind, id)` + the new SECURITY DEFINER `platform.forget_resource` (migration `0012`) clear `resource_principal` + `resource_share` in the plugin's transaction (atomic with the row-delete; no orphans) — the inverse of M08's `record_owner`. **D** ships `PluginContext::public(&ctx)` (sugar over `::<()>::from_rpc` — mostly implicit now that M15's macro feeds the unit alias for unannotated proto methods) and `@junius/sdk` exports `usePluginNavigate` + `PluginLink` (raw-path escape hatches over `buildRoutes`' type-erased subtree); `events` deleted its local `nav.ts`. **A** (the riskiest) extends `junius sync` to manage two more files: a `junius managed` block in `buf.yaml` (proto-module list, marker pattern like Cargo.toml) and a targeted text-edit of the host frontend's `package.json` `"dependencies"` span (top-level layout preserved; non-plugin deps untouched). After non-dry-run sync, if either file actually changed, sync runs the follow-up subprocess: `pnpm exec buf generate` (buf.yaml) and `pnpm install` (package.json). Both managed files are optional — a deployment without them (some test fixtures) skips silently. `sync --dry-run` stays read-only and drift-meaningful. |
| 2026-05-27 | M17 wired routine, per-plugin Playwright E2E. **Discovery** is glob-based — `plugins/*/frontend/e2e/**/*.spec.ts` + `e2e/cross/**/*.spec.ts`; no central registration. The locale + pseudo specs M14 added moved into `e2e/cross/` since they're cross-plugin host journeys; `events` ships the first per-plugin specs (a harness-smoke + the ICS endpoint negative cases as live tests, plus four `.fixme` scaffolds for the UI-driven M13-walk journeys). **Fixtures** live in a new workspace package `@junius/e2e`: `loginAs(name, { permissions })` upserts a `platform.user` (synthetic `e2e:<name>` oidc_sub), creates a fresh per-call group/role with the requested permission set, inserts a `platform.session` row, and sets the `session` cookie on the Playwright `BrowserContext` — so specs start already logged in with **no Authentik UI round-trip** (the M13 dogfooding's biggest provisioning cost). **Stack lifecycle** uses the `testcontainers` npm package directly from Playwright `globalSetup`: per-run ephemeral Postgres + RabbitMQ + MinIO + mailpit, every container labeled `junius-e2e-run=<uuid>` so `globalTeardown` (or `task test:e2e:clean`) can sweep deterministically even after a crash. **Authentik is intentionally absent** from the E2E stack — session-seed removes the dependency. The host runs as the **embedded** build (`--features embed-frontend`) on a fixed local port (`18888`), single-origin (no Vite proxy), with a generated `e2e/host-config.toml` pointing at the testcontainers URLs (literals interpolated from `e2e/host-config.toml.tmpl`, not `env:` indirections). **CI** gains a **sixth** parallel job `e2e` (lint / check / integration / deployment / i18n / **e2e**) — no `services:` block because globalSetup orchestrates containers directly; the runner's Docker is enough. Playwright browser cache keyed on `pnpm-lock.yaml`, trace artifacts uploaded on failure. The old `JUNIUS_E2E=1` gate is gone: `task test:e2e` (and `task ci`) auto-skip only when `docker info` fails locally; CI always runs E2E. |
| 2026-05-28 | M18 (stages 1–4) added the platform`s management layer. **Stage 1**: a new global-scope `user_role` axis (alongside the per-group `group_role` from M06) with a builtin `admin` role holding the literal `'*' permission; `User.is_admin()` short-circuits both `has_permission(p)` and `has_permission_in_group(g, p)`; `platform.user_can_access` adds a top-level admin fast-path that bypasses every ACL evaluation for users with the wildcard. `group_membership` grows `managed_by` (`manual` / `oidc` / `config`) + `managed_source` for provenance. **Stage 2**: the **`admin` plugin** is a first-party plugin (not host UI — preserves the M02 "host has no UI of its own" architecture and dogfoods that the plugin contract is rich enough to manage the platform), gated by a new `platform.admin` capability whose **host-side allowlist** refuses to boot if anything other than `admin` declares it. Cross-schema writes flow through a typed `PlatformAdminApi` accessor on `PluginResources` (no plugin gets direct write on `platform.group*`/`user_role*`); every mutation audit-emits. Four RPC services (GroupAdmin, UserRoleAdmin, PermissionCatalog, UserAdmin) + four FE pages. The **permission catalogue** is sourced from the host`s compiled-in `PluginMetadata::permissions` so the UI can never drift from what `junius check` accepts. **Stage 3**: OIDC `groups` claim reconciled at every login (and via `POST /api/me/refresh-groups` for webhook/n8n flows that don`t wait for re-login) against an admin-UI-managed `oidc_group_mapping` table. The reconciler is provenance-aware — `managed_by='oidc'` rows are freely added/reaped, manual/config rows are explicitly untouched (`WHERE managed_by='oidc'` clauses on both the upsert and the reaper). **Stage 4**: declarative `[provisioning]` block in `platform.toml` (or `file = "…"` pointer) hashed (blake3) and reconciled by `junius provision apply`; re-running with unchanged content is a single short-circuit message via `platform.provisioning_state.last_hash`. `dev/dev-seed.sql` was deleted in favour of `dev/provisioning.toml`. Two seams stay deferred behind well-defined interfaces: `lock_managed` runtime enforcement at `PlatformAdminApi` (flag parsed + persisted but mutator-level rejection of `managed_by='config'` rows TBD) and `auto_apply_on_boot` host-startup integration (`server::run` doesn`t yet invoke apply after `migrate up`). Connect-RPC self-refresh + `junius oidc resync` CLI are similarly deferred — the REST endpoint + admin UI cover the primary operational paths. |
| 2026-05-28 | M18 follow-up + M23 stages 1–5. **M18**: closed both formerly-deferred items — `auto_apply_on_boot` now runs `junius_provision::apply` from `server::run` after the platform pool is built (hash-guarded; reconcile + the CLI share one `junius-provision` crate so they hit the same `platform.provisioning_state` row); `lock_managed = true` (default) is enforced at the `PlatformAdminApi` seam (`add_group_member`/`remove_group_member` consult `group_membership.managed_by` and refuse with the new `PluginError::ManagedByConfig` when the row is `'config'`). Plumbed via `HostConfig.provisioning` → `HostInfra.admin_lock_managed` → constructor parameter. The mutual-source `junius-provision` crate replaced the inline-in-`tools/junius` reconcile module; tests cover the lock matrix (config/manual rows × locked/unlocked × add/remove). **M23 (Optional SSR)**: dual deployment topology landed (stages 1–5; CI gate is stage 6). New `[build] frontend = "embedded" | "none"` selects between the embedded SPA (today's default, unchanged) and a headless API-only binary; the latter returns a structured 404 (`FRONTEND_NOT_EMBEDDED`) on every browser route, paired with a new sibling workspace package `platform/frontend-ssr/` that hosts the React 19 SSR pipeline. **Spike outcome (hand-rolled over TanStack Start)**: our `routeTree` is code-generated by `junius sync` and Start's default convention is file-based routing under `src/routes/` (with 22+ deps incl. babel/rolldown/lightningcss/router-generator on a fast-moving 1.168.x cadence); hand-rolled `renderToPipeableStream` + per-request `createMemoryHistory`/`createRouter` is ~100 LOC plus a Vite SSR config and keeps the plugin-contributed-routes contract intact. The provider tree extracted out of `platform/frontend/src/main.tsx` into a shared `AppShell` component that takes the per-mount `router` + `Transport` as props; the SPA entry and the SSR entry both render the same shell. **Cookie forwarding**: a Node `AsyncLocalStorage` carries the inbound `Cookie` + `Accept-Language` across the per-request async chain; a Connect interceptor on the SSR transport reads from the store on each BE call so SSR runs as the requesting user. **Proxy**: the Node server forwards `/api`, `/h`, `/rpc` to `JUNIUS_BE_INTERNAL_URL` with explicit request- and response-header allowlists (no blanket forwarding). **Dev**: `junius dev --frontend ssr` swaps Vite-SPA for the SSR Node server (`pnpm --filter @junius/shell-ssr dev`) and auto-detects from `[build] frontend = "none"`; embedded mode is unchanged. **Example deployment**: `examples/example-deployment-split/` shows the two-container topology + a comparison table with `examples/example-deployment/`. Embedded stays the supported default; split is additive. |
| 2026-05-28 | M24 Stage 3 audit — what `junius sync` generates, classified for precompiled-mode compatibility. Every artifact `sync` emits depends **only on the enabled plugin set**, never on `[config]` values, per-plugin override tables, or secrets. Conclusion: **bundle-all is sufficient; no refactoring needed for precompiled images.** Artifacts inventoried: (1) `platform/src/generated/plugins.rs` — Rust plugin registry; (2) `platform/src/generated/rpc_requires.rs` — RPC permission graph from proto annotations; (3) `platform/frontend/src/generated/routes.ts` — TanStack Router composed tree; (4) `platform/frontend/src/generated/component-registry.ts` — exposed components map; (5) `platform/frontend/src/generated/i18n-catalogs.ts` — per-plugin Lingui loaders; (6) `crates/junius-sdk/src/generated/domains.rs` — i18n domain enum; (7) `packages/generated/src/proto/<plugin>/` — TS proto codegen; (8) `packages/generated/src/plugins/<plugin>/rpc.ts` — narrow RPC namespaces. `platform/frontend-ssr/` (M23) has zero deployment-baked artifacts — it consumes `@junius/shell/routes` directly. The M24 spec's proposed `BUNDLED_PLUGINS: &[&str]` const is **not needed**: the boot check reads names off the live `Vec<Box<dyn Plugin>>` via `PluginMetadata::name()`. A future generator addition that *would* depend on config values must move to runtime; this audit ratchets the constraint. |
