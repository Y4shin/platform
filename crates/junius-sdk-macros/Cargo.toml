[package]
name         = "junius-sdk-macros"
version      = "0.0.0"
edition.workspace      = true
license.workspace      = true
rust-version.workspace = true
publish      = false

[lib]
proc-macro = true

[dependencies]
junius-manifest = { workspace = true }
junius-rpc-meta = { workspace = true }

proc-macro2 = { workspace = true }
quote       = { workspace = true }
syn         = { workspace = true }
toml        = { workspace = true }

[dev-dependencies]
# proc-macro crates need a real syn for unit tests in addition to the
# proc-macro context — already pulled in above.

[lints]
workspace = true
