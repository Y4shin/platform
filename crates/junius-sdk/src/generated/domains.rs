// Generated by `junius sync` from <deployment>/platform.toml.
// DO NOT EDIT BY HAND. Re-runs of `junius sync` overwrite this file.

/// The set of installed plugin domains for this deployment. `Platform = 0`
/// is reserved for the host's own catalog (deployment-wide strings); the
/// remaining variants follow the order of `[plugins].enabled` in the
/// deployment `platform.toml`.
#[repr(usize)]
#[derive(Copy, Clone, Debug, PartialEq, Eq, Hash)]
pub enum Domain {
    Platform = 0,
    Events = 1,
    Admin = 2,
}

impl Domain {
    pub const COUNT: usize = 3;
    pub const ALL: [Domain; Self::COUNT] = [Self::Platform, Self::Events, Self::Admin];

    /// Catalog domain name as it appears in `.po` headers and in the
    /// build helper's `Options.domain` field.
    #[must_use]
    pub const fn name(self) -> &'static str {
        match self {
            Self::Platform => "platform",
            Self::Events => "events",
            Self::Admin => "admin",
        }
    }

    /// Parse a domain name. `None` for unknown names — callers should
    /// treat that as a configuration error. `const` so the build-time
    /// codegen can emit `const __DOMAIN: Domain = ...`.
    #[must_use]
    pub const fn from_name(s: &str) -> Option<Self> {
        let b = s.as_bytes();
        if matches_bytes(b, b"platform") {
            Some(Self::Platform)
        } else if matches_bytes(b, b"events") {
            Some(Self::Events)
        } else if matches_bytes(b, b"admin") {
            Some(Self::Admin)
        } else {
            None
        }
    }
}

/// `const`-context byte-slice equality. The standard `PartialEq` impl on
/// `&[u8]` is not yet `const`-callable; this small helper is.
const fn matches_bytes(a: &[u8], b: &[u8]) -> bool {
    if a.len() != b.len() {
        return false;
    }
    let mut i = 0;
    while i < a.len() {
        if a[i] != b[i] {
            return false;
        }
        i += 1;
    }
    true
}
